{
  "apiVersion": 1,
  "changes": [
    {
      "classification": "additive-compatible",
      "description": "Initial apiVersion 1 envelope wraps prior raw JSON payloads under data and adds command/outcome metadata."
    },
    {
      "classification": "additive-compatible",
      "description": "Adds the doctor command envelope for read-only source-checkout diagnostics."
    },
    {
      "classification": "additive-compatible",
      "description": "Adds the eval.run command envelope for governed eval verdicts returned through RuntimeApi.runEval."
    }
  ]
}
