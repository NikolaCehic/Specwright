{
  "id": "fixture.budget-within-limit",
  "description": "Allows fs.read when requested budget remains within the configured ceiling.",
  "toolPolicy": {
    "fs.read": {
      "default": "allow",
      "risk": "low",
      "reason": "fs.read is allowed while budget remains available",
      "allowedPhases": [
        "evidence"
      ],
      "requiredScopes": [
        "workspace:read"
      ],
      "allowedScopes": [
        "workspace:read"
      ]
    }
  },
  "budgetPolicy": [
    {
      "id": "budget.file_read_bytes.deny_max",
      "resource": "fileReadBytes",
      "max": 1000,
      "onExceeded": "deny",
      "reason": "file read budget would exceed deny limit"
    }
  ]
}
