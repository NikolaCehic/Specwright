{
  "id": "fixture.shell-exec-rejected-approval",
  "description": "Requires approval for shell execution and leaves rejected decisions ineffective.",
  "toolPolicy": {
    "shell.exec": {
      "default": "approval_required",
      "risk": "high",
      "reason": "shell.exec requires explicit approval",
      "approvalId": "appr_shell_exec_tests",
      "allowedPhases": [
        "execution",
        "verification"
      ],
      "requiredScopes": [
        "shell:exec"
      ],
      "allowedScopes": [
        "shell:exec"
      ],
      "constraints": [
        {
          "kind": "timeoutMs",
          "value": 120000
        },
        {
          "kind": "network",
          "value": "disabled"
        }
      ]
    }
  }
}
